---
interface Props {
  title: string;
}
const { title } = Astro.props;
---
<!doctype html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400;500&display=swap" rel="stylesheet" />
    <title>{title}</title>
    <style is:global>
      *, *::before, *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      html {
        font-size: 18px;
        -webkit-text-size-adjust: 100%;
      }

      body {
        background: #F5C800;
        color: #1A1A1A;
        font-family: 'Noto Sans SC', system-ui, -apple-system, sans-serif;
        font-weight: 300;
        line-height: 1.7;
        padding: 0 clamp(1rem, 5vw, 1.5rem);
      }
    </style>
    <style>
      .site-header {
        max-width: 56rem;
        margin: 0 auto;
        padding: 2rem 0 1.5rem;
      }

      .site-title {
        font-size: 1rem;
        font-weight: 500;
        letter-spacing: 0.02em;
        text-decoration: none;
        color: #1A1A1A;
        display: inline-flex;
        align-items: center;
        gap: 0.45rem;
      }

      .site-title:hover {
        color: #4A3A00;
      }

      .site-logo {
        width: 32px;
        height: 32px;
        image-rendering: pixelated;
      }

      main {
        max-width: 56rem;
        margin: 0 auto;
      }

      footer {
        max-width: 56rem;
        margin: 4rem auto 2rem;
        padding-top: 1.5rem;
        border-top: 2px solid #1A1A1A;
        font-size: 0.8rem;
        color: #4A3A00;
      }
    </style>
  </head>
  <body>
    <header class="site-header">
      <a href={import.meta.env.BASE_URL} class="site-title">
        <img class="site-logo" src={`${import.meta.env.BASE_URL}/icons/Animals-Fox--Streamline-Pixel.svg`} alt="" />
        Axiom Thoughts
      </a>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      Axiom Thoughts
    </footer>
  </body>
</html>
